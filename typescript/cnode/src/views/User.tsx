import React, {Component, ReactNode} from "react";

export class User extends Component {
    render(): ReactNode {
        return (
            <div>
                User
            </div>
        );
    }
}
import React, {Component, ReactNode} from "react";

export class Book extends Component {
    render(): ReactNode {
        return (
            <div>
                Book
            </div>
        );
    }
}